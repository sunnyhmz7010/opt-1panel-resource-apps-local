## 产品介绍

**Firefox** 是 Mozilla 开发的开源 Web 浏览器。本应用基于 `jlesage/firefox` 容器镜像，可在 Docker 中运行 Firefox，并通过浏览器访问图形界面。

## 主要功能

- Web 图形界面：通过 5800 端口在浏览器中访问 Firefox。
- 持久化配置：配置、状态、日志和持久文件保存在 `/config`。
- Web File Manager：可选开启网页文件管理。
- CJK 字体和 Web Audio：支持 CJK 字体和 Web Audio。
